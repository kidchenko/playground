distancia = float(input('Digite a distancia: '))
velocidade = float(input('Digite a velocidade: '))
#t = distancia / velocidade

print('O tempo de viagem sera de: %.2f' % (distancia / velocidade))
